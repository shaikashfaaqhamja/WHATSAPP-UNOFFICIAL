# B2C multi-tenant: do NOT set SECRET. Each user connects with their own secret (QR scan once).
PORT=3780
# Leave SECRET unset for multi-tenant. Only set it if you want one global session for everyone.
# SECRET=

SESSION_PATH=./.wwebjs_auth
DELAY_MIN_MS=12000
DELAY_MAX_MS=25000
JITTER_MS=2000

# Railway sets PORT automatically. PUPPETEER_EXECUTABLE_PATH is set in Dockerfile for Chrome.
