# B2C multi-tenant: do NOT set SECRET. Each user connects with their own secret (QR scan once).
PORT=3780
# Leave SECRET unset for multi-tenant. Only set it if you want one global session for everyone.
# SECRET=

SESSION_PATH=./.wwebjs_auth
DELAY_MIN_MS=12000
DELAY_MAX_MS=25000
JITTER_MS=2000

# Railway sets PORT automatically. PUPPETEER_EXECUTABLE_PATH is set in Dockerfile for Chrome.

# Optional: Supabase Edge Function URL for replying to incoming messages (same logic as Meta webhook).
# Example: https://YOUR_PROJECT_REF.supabase.co/functions/v1/inbound-from-unofficial
# Set this so when contacts message you, the app can auto-reply (feedback 1-5, options 1/2/3, AI from product description).
# INBOUND_EDGE_URL=
